import { stats } from "@/lib/data"; export default function Development(){return <main className="section"><div className="container"><span className="section-kicker">DEVELOPMENT DASHBOARD</span><h1>विकासाचा आढावा</h1><div className="stats-grid" style={{background:"#102f35",padding:20,borderRadius:18}}>{stats.map(s=><div className="stat-card" key={s.label}><div className="stat-icon">{s.icon}</div><b>{s.value}</b><span>{s.label}</span></div>)}</div><div className="content-box" style={{marginTop:30}}><h2>योजना प्रगती</h2><p>येथे Firebase जोडणीनंतर योजना-निहाय live progress, targets आणि completion percentage दाखवता येतील.</p></div></div></main>}